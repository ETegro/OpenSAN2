--- uhttpd-cgi.c
+++ uhttpd-cgi.c
@@ -216,7 +216,7 @@
 			     (pi->stat.st_mode & S_IXOTH)) || (ip != NULL)
 			) {
 				/* build environment */
-				clearenv();
+				//clearenv();
 
 				/* common information */
 				setenv("GATEWAY_INTERFACE", "CGI/1.1", 1);
